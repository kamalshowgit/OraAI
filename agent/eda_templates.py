# EDA_HINTS = {
#     "summary": "Generate descriptive statistics using SQL (COUNT, AVG, MIN, MAX)",
#     "missing": "Check for NULL values using COUNT(*) and WHERE column IS NULL",
#     "distribution": "Use GROUP BY to show distribution",
# }
